import React from "react";
import { Card, Row, Input } from 'antd';

const Chip = () => {
    return (
        <div className="chip-container">
            <Card>
                <Row><Input /></Row>
            </Card>
        </div>
    )
}

export default Chip;